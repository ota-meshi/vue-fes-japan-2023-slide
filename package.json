{
  "name": "vue-fes-japan-2023-slide",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "preview-server-start": "npx http-server dist -o http://localhost:8080/",
    "build:gh": "slidev build --base /vue-fes-japan-2023-slide/ --download",
    "dev": "slidev --open",
    "export": "slidev export --timeout 300000",
    "format": "prettier . --write"
  },
  "dependencies": {
    "@slidev/cli": "^0.43.9",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
